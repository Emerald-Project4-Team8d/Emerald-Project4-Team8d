{
  "routes": [
    {
      "method": "GET",
      "path": "/rubric-entries",
      "handler": "rubric-entry.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/rubric-entries/count",
      "handler": "rubric-entry.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/rubric-entries/:id",
      "handler": "rubric-entry.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/rubric-entries",
      "handler": "rubric-entry.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/rubric-entries/:id",
      "handler": "rubric-entry.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/rubric-entries/:id",
      "handler": "rubric-entry.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
